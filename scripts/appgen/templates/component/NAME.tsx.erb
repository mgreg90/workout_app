import React from "react"

import styles from "./<%= args.name %>.module.css"

interface Props {
  // define your props
}

const <%= args.name %>: React.FC<Props> = (props: Props) => {
  return <></>
}

export default <%= args.name %>